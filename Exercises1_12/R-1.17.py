#Had we implemented the scale function(page 25)as follows,does it work
#properly?
#def scale(data,factor):
#   for val in data:
#       val *= factor

#the function won't change data. val is a local variable,it is not
#an entry of data.

